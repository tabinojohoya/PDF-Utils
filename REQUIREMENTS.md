# PDF-Utils 要件定義書

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| アプリ名 | PDF-Utils |
| プラットフォーム | macOS Tahoe (26.x) |
| 開発言語 | Swift 5 (Swift 6 Concurrency対応) |
| UIフレームワーク | SwiftUI |
| 開発環境 | Xcode 26.2 |
| バンドルID | com.example.PDF-Utils |
| 最小動作OS | macOS 26.2 |

## 2. アプリの目的

複数のPDFファイルを選択し、指定した順序で1つのPDFファイルに結合する、または1つのPDFファイルを複数のファイルに分割するmacOSネイティブアプリケーション。PDF のページ構造を自由に操作する「結合」と「分割」を両輪とした PDF ユーティリティ。

## 3. 機能要件

### 3.1 PDF ファイル追加 (FR-001)

| 項目 | 内容 |
|------|------|
| 概要 | ユーザーが結合対象のPDFファイルを追加できる |
| 入力方法 | ファイル選択ダイアログ（複数選択対応） |
| 入力方法 | ドラッグ&ドロップ（複数ファイル対応） |
| 対象形式 | `.pdf` ファイルのみ |
| バリデーション | PDF形式でないファイルは拒否し、エラーメッセージを表示 |

### 3.2 PDF ファイル一覧表示 (FR-002)

| 項目 | 内容 |
|------|------|
| 概要 | 追加されたPDFファイルをリスト形式で表示 |
| 表示項目 | ファイル名、ページ数、ファイルサイズ |
| サムネイル | 各PDFの先頭ページのサムネイルを表示 |

### 3.3 並び替え (FR-003)

| 項目 | 内容 |
|------|------|
| 概要 | 結合順序をユーザーが自由に変更できる |
| 操作方法 | ドラッグ&ドロップによるリスト内並び替え |
| 操作方法 | 「上へ移動」「下へ移動」ボタン |

### 3.4 ファイル削除 (FR-004)

| 項目 | 内容 |
|------|------|
| 概要 | リストから個別のPDFファイルを除外できる |
| 操作方法 | 選択してDeleteキーまたは削除ボタン |
| 一括操作 | 「すべてクリア」ボタンで全件削除 |

### 3.5 PDF プレビュー (FR-005)

| 項目 | 内容 |
|------|------|
| 概要 | 選択したPDFの内容をプレビュー表示 |
| 表示方法 | 右側ペインにページプレビューを表示 |
| ページ送り | 複数ページのPDFはページ送りが可能 |

### 3.6 PDF 結合実行 (FR-006)

| 項目 | 内容 |
|------|------|
| 概要 | リスト内の全PDFを上から順に結合し、1つのPDFとして出力 |
| 保存方法 | NSSavePanelによる保存先選択ダイアログ |
| デフォルト名 | `Merged.pdf` |
| 処理表示 | 結合中はプログレスバーを表示 |
| 完了通知 | 結合完了後に成功メッセージを表示 |
| エラー処理 | 失敗時はエラー内容を表示 |

### 3.7 結合結果プレビュー (FR-007)

| 項目 | 内容 |
|------|------|
| 概要 | 結合完了後、結果のPDFをQuick Lookまたはプレビューで確認できる |
| 操作 | 「結合結果を開く」ボタンで結合済みPDFを開く |

### 3.8 モード切替 (FR-008)

| 項目 | 内容 |
|------|------|
| 概要 | アプリの動作モードを「結合」と「分割」で切り替えられる |
| 操作方法 | ツールバーのセグメントコントロール（`Picker` with `.segmented`） |
| デフォルト | 結合モード |
| 切替時の挙動 | モード切替時にファイルリスト・選択状態はリセットされる |
| 視覚フィードバック | 選択中のモードがハイライトされ、UIが対応レイアウトに遷移 |

### 3.9 分割対象PDFの選択 (FR-009)

| 項目 | 内容 |
|------|------|
| 概要 | 分割モードでは1つのPDFファイルを入力として選択する |
| 入力方法 | ファイル選択ダイアログ（単一選択） |
| 入力方法 | ドラッグ&ドロップ（複数ドロップ時は先頭1ファイルのみ採用） |
| 対象形式 | `.pdf` ファイルのみ |
| ファイル差し替え | 別のPDFをドロップまたは選択すると現在のファイルを置換 |
| バリデーション | 結合モードと同一（非PDF拒否、破損検出、パスワード保護検出） |

### 3.10 分割方法の選択 (FR-010)

| 項目 | 内容 |
|------|------|
| 概要 | PDFの分割方法を複数のプリセットから選択できる |
| ページ範囲指定 | ユーザーが任意のページ範囲を指定して分割（例: 1-3, 4-10, 11-15） |
| 1ページずつ | 全ページを個別のPDFファイルとして書き出し |
| 偶数/奇数分割 | 偶数ページ・奇数ページをそれぞれ別ファイルに分離 |
| デフォルト | ページ範囲指定 |

### 3.11 分割プレビュー (FR-011)

| 項目 | 内容 |
|------|------|
| 概要 | 分割結果を実行前に視覚的に確認できる |
| 表示方法 | 右ペインにページサムネイルグリッドを表示 |
| 分割境界線 | 分割位置に視覚的な区切り線を表示し、各チャンクを色分け |
| チャンク情報 | 各分割グループのページ数を表示 |
| リアルタイム更新 | 分割設定の変更に応じてプレビューが即座に更新される |

### 3.12 分割実行 (FR-012)

| 項目 | 内容 |
|------|------|
| 概要 | 設定に基づいてPDFを分割し、複数のPDFファイルとして出力 |
| 保存方法 | NSOpenPanel（ディレクトリ選択）による保存先フォルダ選択 |
| 出力ファイル名 | `{元ファイル名}_part1.pdf`, `{元ファイル名}_part2.pdf`, ... |
| 処理表示 | 分割中はプログレスバーを表示 |
| 完了通知 | 分割完了後に成功メッセージを表示（出力ファイル数を含む） |
| エラー処理 | 失敗時はエラー内容を表示 |

### 3.13 分割結果の確認 (FR-013)

| 項目 | 内容 |
|------|------|
| 概要 | 分割完了後、出力先フォルダを確認できる |
| 操作 | 「Finderで表示」ボタンで出力先フォルダを開く |

### 3.14 ページ範囲入力 (FR-014)

| 項目 | 内容 |
|------|------|
| 概要 | ページ範囲指定モードで、分割位置を柔軟に入力できる |
| テキスト入力 | カンマ区切りで範囲を指定（例: `1-3, 4-10, 11-15`） |
| バリデーション | 範囲の重複・ページ番号超過・不正フォーマットを検出しエラー表示 |
| 自動補完 | 未指定ページがある場合、残りのページを自動的に末尾グループに追加 |
| ページ総数表示 | 入力フィールド横にPDFの総ページ数を表示（「/ 24ページ」） |

## 4. 非機能要件

### 4.1 パフォーマンス (NFR-001)

- 100ページ以上のPDFでも数秒以内に結合・分割処理を完了すること
- PDF結合・分割処理はバックグラウンドスレッドで実行し、UIをブロックしないこと
- メモリ効率を考慮し、大容量PDFでもメモリ不足にならない設計とすること

### 4.2 セキュリティ (NFR-002)

- App Sandbox有効（macOS標準のセキュリティモデルに準拠）
- ユーザーが明示的に選択したファイルのみアクセス（`ENABLE_USER_SELECTED_FILES`）
- Hardened Runtime有効

### 4.3 ユーザビリティ (NFR-003)

- macOSネイティブのルック&フィールに準拠
- キーボードショートカット対応（⌘O: ファイル追加、⌘S: 結合実行、⌘⌫: 選択削除）
- ダークモード / ライトモード両対応

### 4.4 信頼性 (NFR-004)

- 破損したPDFファイルについて適切なエラーハンドリングを行うこと
- パスワード付きPDFはスキップし、ユーザーに通知すること

## 5. 技術設計方針

### 5.1 アーキテクチャ

```
PDF-Utils/
├── PDF_UtilsApp.swift          # アプリエントリポイント
├── Models/
│   ├── PDFItem.swift            # PDFファイルモデル（ファイル情報保持）
│   └── SplitConfig.swift        # 分割設定モデル（方式・範囲）
├── ViewModels/
│   └── PDFMergeViewModel.swift  # 結合・分割ロジック・状態管理
├── Views/
│   ├── ContentView.swift        # メインレイアウト（Split View）
│   ├── PDFListView.swift        # PDF一覧（左ペイン・結合モード）
│   ├── PDFListRowView.swift     # 一覧の各行
│   ├── PDFPreviewView.swift     # プレビュー（右ペイン）
│   ├── DropOverlayView.swift    # ドラッグ&ドロップオーバーレイ
│   ├── SplitConfigView.swift    # 分割設定UI（左ペイン・分割モード）
│   └── SplitPreviewView.swift   # 分割プレビュー（右ペイン・分割モード）
├── Services/
│   ├── PDFMergeService.swift    # PDF結合エンジン（PDFKit使用）
│   └── PDFSplitService.swift    # PDF分割エンジン（PDFKit使用）
└── Assets.xcassets/
```

### 5.2 使用フレームワーク

| フレームワーク | 用途 |
|---------------|------|
| SwiftUI | UI構築 |
| PDFKit | PDF読み込み・結合・分割・プレビュー |
| UniformTypeIdentifiers | ファイルタイプ判定 |
| Combine / async-await | 非同期処理 |

> **注**: SwiftDataは本アプリでは不要（永続化不要のため）。テンプレートのSwiftData関連コードは削除する。

### 5.3 PDF結合ロジック

```swift
// 概要: PDFKit の PDFDocument を使用
// 1. 各PDFファイルを PDFDocument として読み込み
// 2. 新規 PDFDocument を作成
// 3. 各ソースの全ページを順番に insert
// 4. 結合後の PDFDocument を write で保存
```

### 5.4 PDF分割ロジック

```swift
// 概要: PDFKit の PDFDocument を使用
// 1. 元PDFを PDFDocument(url:) で読み込み
// 2. 分割設定（SplitConfig）に基づきページ範囲グループを算出
// 3. 各グループごとに新規 PDFDocument を作成
// 4. 対象ページを page(at:) で取得し insert(_:at:) で追加
// 5. 各 PDFDocument を write(to:) で個別ファイルとして保存
// 6. グループ完了ごとに進捗を更新
```

### 5.5 分割設定モデル

```swift
/// 分割方式を表す列挙型
enum SplitMethod: String, CaseIterable, Identifiable {
    case pageRanges   // ページ範囲指定
    case everyPage    // 1ページずつ
    case oddEven      // 偶数/奇数

    var id: String { rawValue }
}

/// 分割設定
struct SplitConfig {
    var method: SplitMethod = .pageRanges
    var rangeText: String = ""  // "1-3, 4-10, 11-15"

    /// 設定からページ範囲グループを算出
    func computeGroups(totalPages: Int) -> Result<[[Int]], SplitConfigError>
}
```

### 5.6 状態管理

- `@Observable` クラス（`PDFMergeViewModel`）で一元管理
- `appMode: AppMode`（`.merge` / `.split`）でモード状態を保持
- 分割モード時: `splitSourceItem: PDFItem?`, `splitConfig: SplitConfig` を追加管理
- SwiftData は使用しない（セッション内の一時データのみ）

```swift
/// アプリの動作モード
enum AppMode: String, CaseIterable, Identifiable {
    case merge  // 結合モード
    case split  // 分割モード

    var id: String { rawValue }
}
```

## 6. GUI設計思想

> *"Design is not just what it looks like and feels like. Design is how it works."*
> — Steve Jobs

本アプリのGUIは「**引き算の美学**」に基づく。機能を足すのではなく、ユーザーが考えなくて済むものを一つずつ取り除く。PDFを結合する——たったそれだけのことに、マニュアルは要らない。アプリを初めて開いた瞬間に、次に何をすべきかが分かる。それが正しいデザインである。

### 6.1 デザイン原則

| # | 原則 | 具体的な指針 |
|---|------|-------------|
| 1 | **極限のシンプルさ** | 画面上の全要素に存在理由を問う。説明できないものは削除する |
| 2 | **一目で伝わる階層** | 最も重要なアクション（結合実行）が視覚的に最も目立つこと |
| 3 | **直接操作の快感** | ドラッグ&ドロップは「掴んで、置く」。人間が物を扱う自然な動作そのもの |
| 4 | **待たせない、退屈させない** | 全てのインタラクションに即座のフィードバック。沈黙は最悪のUI |
| 5 | **一貫性という信頼** | macOSネイティブの作法に従う。独自UIは作らない。Appleが正しい |
| 6 | **余白は贅沢ではなく必然** | 詰め込まない。呼吸できる空間がコンテンツを引き立てる |

### 6.2 カラーシステム

独自カラーは定義しない。macOSのセマンティックカラーに100%準拠する。

| 用途 | カラー指定 | 理由 |
|------|-----------|------|
| 背景 | `.background` (System) | OSのモード切替に自動追従 |
| テキスト（主） | `.primary` | 可読性最優先 |
| テキスト（副） | `.secondary` | 情報の階層を自然に伝える |
| アクセント | `.accentColor` (System Blue) | OSの設定をユーザーが変更可能 |
| 結合ボタン | `.tint` + `.borderedProminent` | 画面内で唯一の「主張する色」 |
| ドロップゾーン（アクティブ） | `.blue.opacity(0.08)` + 破線ボーダー | 受け入れ可能状態を穏やかに示す |
| エラー | `.red` (System) | 赤は「注意」の万国共通語 |
| 成功 | `.green` (System) | 完了の安心感 |
| サムネイル背景 | `.quaternary` | コンテンツを邪魔しない最小限の境界 |

**ダークモード**: 全てのセマンティックカラーがOSレベルで自動反転する。カスタム対応コードはゼロ。

### 6.3 タイポグラフィ

システムフォント（San Francisco）のみ使用。カスタムフォントは不要。

| 要素 | スタイル | サイズ区分 |
|------|---------|-----------|
| ウィンドウタイトル | OS標準タイトルバー | — |
| ツールバーラベル | `.body` | 13pt |
| ファイル名 | `.body` `.fontWeight(.medium)` | 13pt |
| ページ数・サイズ | `.caption` `.foregroundStyle(.secondary)` | 10pt |
| 空状態メッセージ（主） | `.title2` `.fontWeight(.medium)` | 17pt |
| 空状態メッセージ（副） | `.subheadline` `.foregroundStyle(.secondary)` | 12pt |
| ステータスバー | `.caption` `.foregroundStyle(.secondary)` | 10pt |
| 結合ボタン | `.body` `.fontWeight(.semibold)` | 13pt |

### 6.4 アイコノグラフィ

SF Symbolsのみ使用。サードパーティアイコンは一切不要。

| 用途 | SF Symbol名 | レンダリング | 理由 |
|------|------------|-------------|------|
| ファイル追加 | `plus` | `.monochrome` | 最も普遍的な「追加」の記号 |
| 削除 | `trash` | `.monochrome` | macOS標準の削除メタファー |
| 全クリア | `trash.slash` | `.monochrome` | 「全部消す」のニュアンス |
| 結合実行 | `doc.on.doc.fill` | `.hierarchical` | 2つの文書→1つへの暗示 |
| 上へ移動 | `chevron.up` | `.monochrome` | 方向の直感 |
| 下へ移動 | `chevron.down` | `.monochrome` | 方向の直感 |
| 空状態 | `doc.richtext` | `.hierarchical` | PDFの象徴。大きく表示 |
| ドラッグヒント | `arrow.down.doc` | `.hierarchical` | 「ここに落とせ」の視覚的合図 |
| エラー | `exclamationmark.triangle.fill` | `.multicolor` | 目を引くが威圧しない |
| 成功 | `checkmark.circle.fill` | `.multicolor` | 達成感のシグナル |
| ページ送り（前） | `chevron.left` | `.monochrome` | — |
| ページ送り（次） | `chevron.right` | `.monochrome` | — |
| 分割実行 | `scissors` | `.monochrome` | 分割の直感的メタファー |
| 分割モード | `rectangle.split.2x1` | `.hierarchical` | 1つが2つに分かれるイメージ |
| 結合モード | `doc.on.doc` | `.hierarchical` | 2つが1つになるイメージ |
| フォルダ選択 | `folder` | `.monochrome` | 保存先ディレクトリの暗示 |
| 分割境界線 | `arrow.down.to.line` | `.monochrome` | ここで区切る、の視覚的合図 |

### 6.5 画面レイアウト詳細

#### 6.5.1 メインウィンドウ構造（結合モード）

```
┌─────────────────────────────────────────────────────────────────┐
│  タイトルバー: PDF-Utils                                 (macOS標準) │
├─────────────────────────────────────────────────────────────────┤
│  ツールバー:                                                     │
│  [ 結合 | 分割 ]  [ + ファイル追加 ]  [ 🗑 削除 ]     [ 📄📄 結合 ]│
│  ← モード切替 →  ← 左寄せ →                     ← 右寄せ・強調 → │
├──────────────────────────┬──────────────────────────────────────┤
│                          │                                      │
│   PDF ファイル一覧        │        PDF プレビュー                  │
│   (NavigationSplitView   │        (Detail Pane)                 │
│    Sidebar)              │                                      │
│                          │   ┌──────────────────────────┐      │
│  ┌────┐                  │   │                          │      │
│  │ 🖼 │  report.pdf      │   │                          │      │
│  │    │  12ページ · 2.4MB │   │    PDFKitRepresentable   │      │
│  └────┘                  │   │    (PDFView)              │      │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │   │                          │      │
│  ┌────┐                  │   │    選択中ファイルの         │      │
│  │ 🖼 │  invoice.pdf     │   │    全ページを表示           │      │
│  │    │  1ページ · 89KB   │   │                          │      │
│  └────┘                  │   │                          │      │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │   └──────────────────────────┘      │
│  ┌────┐                  │                                      │
│  │ 🖼 │  slides.pdf      │                                      │
│  │    │  24ページ · 8.1MB │                                      │
│  └────┘                  │                                      │
│                          │                                      │
├──────────────────────────┴──────────────────────────────────────┤
│  ステータス: 3ファイル · 合計37ページ                              │
└─────────────────────────────────────────────────────────────────┘
```

**レイアウトの鉄則**:
- ツールバーの左側は「素材を扱う操作」（追加・削除）、右側は「成果を生む操作」（結合）。この左→右の流れはワークフローそのもの
- 結合ボタンだけが `.borderedProminent` で、画面全体の中で唯一の「ここを押せ」ポイント
- サイドバーとプレビューの比率は約 **1:2.5**。プレビューが主役であり、リストは脇役

#### 6.5.2 空状態（ゼロステート）— 最も重要な画面

> *"初対面の印象が全て。ユーザーがアプリを開いて最初に見る画面が白紙なら、それは失敗だ。"*

```
┌─────────────────────────────────────────────────────────────────┐
│  タイトルバー: PDF-Utils                                          │
├─────────────────────────────────────────────────────────────────┤
│  ツールバー:  [ + ファイル追加 ]                                   │
│              (他のボタンは非表示。存在しないものに触れさせない)      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                                                                 │
│                                                                 │
│                        ╭─ ─ ─ ─ ─ ─ ─ ─ ─ ─╮                 │
│                        ╎                      ╎                 │
│                        ╎    ↓ 📄              ╎                 │
│                        ╎                      ╎                 │
│                        ╰─ ─ ─ ─ ─ ─ ─ ─ ─ ─╯                 │
│                                                                 │
│                    PDFファイルをここにドロップ                      │
│                                                                 │
│                  またはファイル追加ボタンで選択                     │
│                    (.secondary, .subheadline)                    │
│                                                                 │
│                                                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**ゼロステートの設計意図**:
- ウィンドウ全体がドロップゾーン。「どこに落とせばいいか」と悩ませない
- メッセージは最大2行。3行目が必要なら、設計が間違っている
- アイコンは大きく（48pt）、色は `.secondary`。主張しすぎず、でも確実に目に入る
- ``NavigationSplitView`` は非表示。Split Viewの空っぽの2ペインほど寂しいものはない
- ツールバーには「ファイル追加」ボタンのみ。削除も結合もまだ意味がない

#### 6.5.3 ドラッグ中のフィードバック

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   ╔══════════════════════════════════════════════════════════╗  │
│   ║                                                          ║  │
│   ║              .blue.opacity(0.08) 背景                    ║  │
│   ║              + 破線ボーダー (.blue.opacity(0.4))          ║  │
│   ║                                                          ║  │
│   ║                  ↓ 📄                                    ║  │
│   ║                                                          ║  │
│   ║            ドロップしてPDFを追加                           ║  │
│   ║                                                          ║  │
│   ╚══════════════════════════════════════════════════════════╝  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**ドラッグ&ドロップの挙動**:
- ファイルがウィンドウ上に差し掛かった瞬間、全体がドロップゾーンに変化
- 背景色が穏やかなブルーに遷移（`.animation(.easeInOut(duration: 0.2))`）
- 受け入れ可能なファイルのみハイライト、非PDFは無反応（視覚的拒絶ではなく、静かな無視）
- ドロップ完了時、追加されたファイルがリストにスライドイン（`.transition(.move(edge: .leading).combined(with: .opacity))`）

#### 6.5.3.1 分割モードのドラッグ&ドロップ

- 結合モードと同一の視覚フィードバック
- 複数ファイルがドロップされた場合、先頭1ファイルのみ採用（残りは無視）
- 既にファイルが読み込まれている場合、新しいファイルで置換（確認不要）

#### 6.5.4 リスト行（PDFListRowView）の詳細設計

```
┌──────────────────────────────────┐
│                                  │
│  ┌────────┐  report_final.pdf    │  ← .body .fontWeight(.medium)
│  │        │  12ページ · 2.4 MB   │  ← .caption .secondary
│  │ サムネ │                      │
│  │  イル  │                      │
│  │ 44x56  │                      │
│  └────────┘                      │
│                                  │
└──────────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| サムネイル | PDFの1ページ目を描画。44×56pt（A4比率）。角丸2pt。薄い影 |
| ファイル名 | 長い名前は末尾 `...` で省略。ホバーでフル名をツールチップ表示 |
| メタ情報 | `{ページ数} · {ファイルサイズ}` を中黒区切りで1行表示 |
| 行間隔 | 8pt のパディング。密すぎず、疎すぎない |
| 選択状態 | macOS標準のリスト選択ハイライト（`.accentColor` 背景） |
| 並び替え | 行をドラッグすると半透明になり、ドロップ位置にインジケータ線を表示 |

#### 6.5.5 プレビューペイン（PDFPreviewView）

| 要素 | 仕様 |
|------|------|
| 実装 | `PDFView` を `NSViewRepresentable` でラップ |
| 表示モード | `.singlePage` をデフォルト、ユーザーが連続スクロールに切替可能 |
| 自動フィット | `.autoScales = true` でペインサイズに自動フィット |
| ページ遷移 | PDFView標準のスクロール / スワイプ |
| 未選択時 | 薄いグレー背景 + 「PDFを選択してプレビュー」テキスト（`.secondary`） |
| ズーム | ピンチ or ⌘+/⌘- で拡大縮小 |

#### 6.5.6 ステータスバー

```
結合モード:
┌──────────────────────────────────────────────────────────────┐
│  📄 3ファイル · 合計37ページ                                   │
│                                   ═══════════░░░░░ 60%       │
│                                   (結合中のみ表示)              │
└──────────────────────────────────────────────────────────────┘

分割モード:
┌──────────────────────────────────────────────────────────────┐
│  ✂️ report.pdf · 24ページ → 3ファイルに分割                    │
│                                   ═══════════░░░░░ 60%       │
│                                   (分割中のみ表示)              │
└──────────────────────────────────────────────────────────────┘
```

- 結合通常時: ファイル数と合計ページ数のみ。静かに佇む
- 分割通常時: ファイル名、総ページ数、分割後のファイル数を表示
- 処理中: 右側にプログレスバーが `.transition(.opacity)` で出現
- 結合完了時: ✅ アイコン + 「結合完了」テキストが3秒間表示され、フェードアウト
- 分割完了時: ✅ アイコン + 「分割完了 — {n}ファイルを出力」テキストが3秒間表示され、フェードアウト

#### 6.5.7 完了ダイアログ

> *"成功は祝え。でも大げさにするな。"*

モーダルダイアログは使わない。画面上部にトースト風バナーをスライドイン表示。

```
結合完了:
┌─────────────────────────────────────────────────┐
│  ✅  結合が完了しました   [ Finderで表示 ]  [ ✕ ] │
└─────────────────────────────────────────────────┘

分割完了:
┌──────────────────────────────────────────────────────────┐
│  ✅  3ファイルに分割しました  [ Finderで表示 ]  [ ✕ ]      │
└──────────────────────────────────────────────────────────┘
```

- 5秒後に自動消滅（`.animation(.easeOut)`）
- 「Finderで表示」ボタンで保存先（結合: ファイル、分割: フォルダ）を即座に確認可能
- ユーザーの作業フローを邪魔しない

#### 6.5.8 分割モード — メインウィンドウ構造

```
┌─────────────────────────────────────────────────────────────────┐
│  タイトルバー: PDF-Utils                                 (macOS標準) │
├─────────────────────────────────────────────────────────────────┤
│  ツールバー:                                                     │
│  [ 結合 | *分割* ]  [ + ファイル選択 ]              [ ✂️ 分割 ]   │
│  ← モード切替 →    ← 左寄せ →                ← 右寄せ・強調 →    │
├──────────────────────────┬──────────────────────────────────────┤
│                          │                                      │
│   分割設定                │        分割プレビュー                  │
│   (SplitConfigView)      │        (SplitPreviewView)            │
│                          │                                      │
│  ┌────┐                  │   ┌───┐ ┌───┐ ┌───┐ ┌───┐          │
│  │ 🖼 │  report.pdf      │   │ 1 │ │ 2 │ │ 3 │ │ 4 │          │
│  │    │  24ページ · 4.2MB │   └───┘ └───┘ └───┘ └───┘          │
│  └────┘                  │   ══════ Part 1 (4p) ══════          │
│                          │   ┌───┐ ┌───┐ ┌───┐                │
│  分割方法:               │   │ 5 │ │ 6 │ │ 7 │ ...            │
│  ● ページ範囲指定         │   └───┘ └───┘ └───┘                │
│  ○ 1ページずつ           │   ══════ Part 2 (6p) ══════          │
│  ○ 偶数/奇数             │   ┌───┐ ┌───┐                      │
│                          │   │ 8 │ │ 9 │ ...                  │
│  ページ範囲:             │   └───┘ └───┘                      │
│  ┌──────────────┐        │   ══════ Part 3 (14p) ═════          │
│  │ 1-4, 5-10,   │ /24   │                                      │
│  │ 11-24         │       │                                      │
│  └──────────────┘        │                                      │
│                          │                                      │
├──────────────────────────┴──────────────────────────────────────┤
│  ✂️ report.pdf · 24ページ → 3ファイルに分割                      │
└─────────────────────────────────────────────────────────────────┘
```

**分割モードのレイアウト原則**:
- 左ペインは「入力（1ファイル）+ 設定」。結合モードの「複数ファイルリスト」に相当
- 右ペインは「分割結果のプレビュー」。ページサムネイルグリッドに分割境界線を重畳
- 分割ボタンだけが `.borderedProminent`。結合モードと同じ「唯一の主張する色」ルールを踏襲
- 分割方法のラジオボタンは macOS ネイティブの `Picker(.radioGroup)` を使用
- ページ範囲入力は `TextEditor` で複数行対応。右に総ページ数を `.secondary` で表示

#### 6.5.9 分割モード — 空状態（ファイル未選択）

```
┌─────────────────────────────────────────────────────────────────┐
│  タイトルバー: PDF-Utils                                          │
├─────────────────────────────────────────────────────────────────┤
│  ツールバー:  [ 結合 | *分割* ]  [ + ファイル選択 ]                 │
│              (分割ボタンは非表示。ファイルがないので意味がない)      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                                                                 │
│                        ╭─ ─ ─ ─ ─ ─ ─ ─ ─ ─╮                 │
│                        ╎                      ╎                 │
│                        ╎    ↓ 📄              ╎                 │
│                        ╎                      ╎                 │
│                        ╰─ ─ ─ ─ ─ ─ ─ ─ ─ ─╯                 │
│                                                                 │
│                  分割するPDFファイルをここにドロップ                │
│                                                                 │
│                  またはファイル選択ボタンで選択                     │
│                    (.secondary, .subheadline)                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**分割モード空状態の設計意図**:
- 結合モードの空状態と同じデザイン言語を踏襲（一貫性）
- メッセージのみ「分割する」に変更。レイアウト・サイズ・色は同一
- モード切替ピッカーは常に表示（どのモードにいるかを常に示す）

### 6.6 アニメーション設計

> *"アニメーションは装飾ではない。因果関係を伝える言語だ。"*

| トリガー | アニメーション | Duration | Curve |
|---------|-------------|----------|-------|
| ファイル追加 | リストにスライドイン | 0.3s | `.spring(response: 0.3, dampingFraction: 0.8)` |
| ファイル削除 | リストからフェード+縮小して消滅 | 0.25s | `.easeOut` |
| 並び替え | 行が移動先へスムーズに移動 | 0.3s | `.spring(response: 0.3, dampingFraction: 0.7)` |
| ドラッグ進入 | ドロップゾーンが穏やかに発光 | 0.2s | `.easeInOut` |
| ドラッグ退出 | ドロップゾーンがフェードアウト | 0.15s | `.easeOut` |
| プレビュー切替 | クロスフェード | 0.2s | `.easeInOut` |
| 結合開始 | プログレスバーがスライドイン | 0.3s | `.easeOut` |
| 結合完了 | 成功バナーが上からスライドイン | 0.4s | `.spring(response: 0.4, dampingFraction: 0.75)` |
| 成功バナー消滅 | フェードアウト | 0.5s | `.easeOut`（5秒後に発火） |
| 空状態→リスト表示切替 | 空状態がフェードアウト→SplitViewが展開 | 0.35s | `.easeInOut` |
| モード切替 | 左右ペインのコンテンツがクロスフェード | 0.25s | `.easeInOut` |
| 分割境界線表示 | 区切り線がフェードイン | 0.2s | `.easeInOut` |
| 分割設定変更 | プレビューのグループ分けがアニメーション更新 | 0.3s | `.spring(response: 0.3, dampingFraction: 0.8)` |

**アニメーションの鉄則**:
- 0.5秒を超えるアニメーションは存在しない。ユーザーを待たせる演出は傲慢
- 全てのアニメーションに物理的な意味がある（上から落ちる、横からスライドする など）
- `withAnimation {}` で明示的にトリガーし、意図しない箇所が動かないようにする

### 6.7 インタラクションの状態遷移

```
#### 結合モード

```
                    ┌─────────────┐
                    │   空状態     │
                    │ (ゼロステート) │
                    └──────┬──────┘
                           │ ファイル追加（D&D or ダイアログ）
                           ▼
                    ┌─────────────┐
            ┌──────▶│  編集状態    │◀────── 並び替え
            │       │ (ファイル有) │◀────── 追加
            │       └──────┬──────┘─────▶ 削除（残0件で空状態へ）
            │              │
            │              │ 結合ボタン押下
            │              ▼
            │       ┌─────────────┐
            │       │  結合処理中   │  ← UIロック（追加・削除・並替え無効）
            │       │              │  ← プログレスバー表示
            │       └──────┬──────┘
            │              │
            │         ┌────┴────┐
            │         ▼         ▼
            │  ┌──────────┐ ┌──────────┐
            │  │  成功     │ │  失敗     │
            │  │ バナー表示 │ │ エラー表示 │
            │  └─────┬────┘ └─────┬────┘
            │        │            │
            └────────┴────────────┘
                 編集状態に復帰
```

#### 分割モード

```
                    ┌─────────────┐
                    │   空状態     │
                    │ (ファイル未選択)│
                    └──────┬──────┘
                           │ ファイル選択（D&D or ダイアログ）
                           ▼
                    ┌─────────────┐
            ┌──────▶│  設定状態    │◀────── 分割方法変更
            │       │ (ファイル有) │◀────── ページ範囲変更
            │       └──────┬──────┘─────▶ ファイル差し替え
            │              │
            │              │ 分割ボタン押下
            │              ▼
            │       ┌─────────────┐
            │       │  分割処理中   │  ← UIロック（設定変更無効）
            │       │              │  ← プログレスバー表示
            │       └──────┬──────┘
            │              │
            │         ┌────┴────┐
            │         ▼         ▼
            │  ┌──────────┐ ┌──────────┐
            │  │  成功     │ │  失敗     │
            │  │ バナー表示 │ │ エラー表示 │
            │  └─────┬────┘ └─────┬────┘
            │        │            │
            └────────┴────────────┘
                 設定状態に復帰
```

#### モード間遷移

```
┌─────────────┐   ツールバーのモード切替ピッカー   ┌─────────────┐
│  結合モード   │◀─────────────────────────────▶│  分割モード   │
│ (どの状態でも)│   切替時にファイル・設定はリセット  │ (どの状態でも)│
└─────────────┘                               └─────────────┘
```

### 6.8 レスポンシブ対応

| ウィンドウ幅 | レイアウト変化 |
|-------------|-------------|
| 900pt以上（通常） | サイドバー + プレビューの2ペイン構成 |
| 600〜899pt | サイドバー幅を縮小、サムネイルを非表示にし、テキストのみ表示 |
| 600pt未満 | 最小サイズ制限によりこれ以下にはならない |

### 6.9 アクセシビリティ

> *"すべての人のためにデザインする。例外はない。"*

| 要素 | 対応 |
|------|------|
| VoiceOver | 全ての操作要素に `.accessibilityLabel` を付与 |
| サムネイル | `"PDFの1ページ目のプレビュー"` をラベル設定 |
| ファイル行 | `"{ファイル名}、{ページ数}ページ、{サイズ}"` を読み上げ |
| ドラッグ | `.accessibilityAction` で「上に移動」「下に移動」を提供 |
| 結合ボタン | `"PDFを結合"` + ファイル数を `.accessibilityValue` に |
| 分割ボタン | `"PDFを分割"` + 分割先ファイル数を `.accessibilityValue` に |
| モード切替 | `"動作モード"` をグループラベルに設定 |
| 分割方法 | 各ラジオボタンに `"ページ範囲指定"` `"1ページずつ"` `"偶数奇数"` を設定 |
| 範囲入力 | `"分割ページ範囲"` + バリデーション結果を `.accessibilityHint` に |
| ステータス | `.accessibilityLiveRegion(.polite)` で変化を自動通知 |
| フォーカス | Tab キーで全操作要素を順に移動可能 |
| Dynamic Type | macOSのテキストサイズ設定に追従 |
| カラーコントラスト | WCAG AA準拠（セマンティックカラー使用により自動達成） |

## 7. ウィンドウ仕様

| 項目 | 値 | 理由 |
|------|-----|------|
| 初期サイズ | 960 × 640 | 16:10.67 — MacBookの画面比率に調和 |
| 最小サイズ | 600 × 400 | これ未満ではプレビューが意味を失う |
| リサイズ | 可能 | macOSの標準操作を阻害しない |
| Split View比率 | 左280pt固定最小幅 : 右は残り全て | プレビューが常に主役 |
| ツールバースタイル | `.unified` | コンテンツとの一体感 |
| タイトルバー | 表示（アプリ名のみ） | ドキュメント名は不要（プロジェクト型ではない） |
| フルスクリーン | 対応 | macOSの標準機能を制限しない |

## 8. 対応ファイル形式

| 形式 | 対応 |
|------|------|
| PDF (.pdf) | ○ 入出力 |
| パスワード付きPDF | △ スキップ（通知表示） |
| 破損PDF | × エラー表示して除外 |

## 9. キーボードショートカット

| ショートカット | 動作 |
|---------------|------|
| ⌘ O | ファイル追加ダイアログを開く（両モード共通） |
| ⌘ S | PDF結合を実行（結合モード） / PDF分割を実行（分割モード） |
| ⌘ ⌫ | 選択中のファイルをリストから削除（結合モード） |
| ⌘ A | リスト内の全ファイルを選択（結合モード） |
| ⌘ 1 | 結合モードに切替 |
| ⌘ 2 | 分割モードに切替 |

## 10. 制約事項・前提条件

- macOS Tahoe (26.x) 以降で動作
- App Sandbox環境下で動作するため、ユーザーが明示的に選択したファイルのみアクセス可能
- 外部ライブラリへの依存なし（Apple純正フレームワークのみ使用）
- SwiftDataは不使用（永続化要件なし）

## 11. 実装手順（v1.0 結合機能）

全12ステップ。各ステップは前のステップの成果物に依存するため、順序厳守。
各ステップ完了時にビルド確認を行い、コンパイルが通る状態を維持する。

---

### Step 1: プロジェクトクリーンアップ — テンプレートコードの除去

**目的**: SwiftDataテンプレートを完全に除去し、白紙の状態にする

**対象ファイル**:
| ファイル | 操作 |
|---------|------|
| `Item.swift` | 削除 |
| `PDF_UtilsApp.swift` | SwiftData依存を除去、シンプルな `WindowGroup` のみに |
| `ContentView.swift` | SwiftData依存を除去、空のビューに |

**変更内容**:

`PDF_UtilsApp.swift`:
```swift
import SwiftUI

@main
struct PDF_UtilsApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 960, height: 640)
    }
}
```

`ContentView.swift`:
```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("PDF-Utils")
    }
}
```

**完了条件**: ビルド成功。空ウィンドウが表示される。

---

### Step 2: データモデル — PDFItem の定義

**目的**: PDFファイル1件を表すモデルを作成

**作成ファイル**: `Models/PDFItem.swift`

**モデル設計**:
```swift
import Foundation
import PDFKit
import SwiftUI

struct PDFItem: Identifiable, Hashable {
    let id = UUID()
    let url: URL
    let fileName: String
    let pageCount: Int
    let fileSize: Int64
    let thumbnail: NSImage

    // URLからファクトリ生成
    // PDFDocument で読み込み、ページ数・サムネイルを取得
    // ファイルサイズは FileManager で取得
}
```

**重要な設計判断**:
- `Identifiable` 準拠 → SwiftUI の `ForEach` / `List` で使用
- `Hashable` 準拠 → `NavigationSplitView` の選択管理で使用
- サムネイルは生成時に1回だけ描画しキャッシュ（毎フレーム描画しない）
- ファイルサイズの人間可読フォーマット用ヘルパーを用意（`ByteCountFormatter`）

**完了条件**: ビルド成功。モデル単体でコンパイルが通る。

---

### Step 3: ViewModel — PDFMergeViewModel の作成

**目的**: 全画面状態を一元管理する `@Observable` クラスを作成

**作成ファイル**: `ViewModels/PDFMergeViewModel.swift`

**責務**:
```swift
@Observable
class PDFMergeViewModel {
    // --- 状態 ---
    var pdfItems: [PDFItem] = []
    var selectedItemID: PDFItem.ID? = nil
    var isDropTargeted: Bool = false
    var isMerging: Bool = false
    var mergeProgress: Double = 0.0
    var alertMessage: String? = nil
    var showSuccessBanner: Bool = false
    var savedFileURL: URL? = nil

    // --- 算出プロパティ ---
    var totalPageCount: Int
    var isEmpty: Bool
    var canMerge: Bool  // items.count >= 2
    var selectedItem: PDFItem?

    // --- 操作 ---
    func addFiles(urls: [URL])         // PDF読み込み + バリデーション
    func removeItems(ids: Set<UUID>)   // 選択削除
    func clearAll()                    // 全件削除
    func moveItem(from:to:)            // 並び替え
    func merge() async                 // 結合実行（非同期）
    func revealInFinder()              // 結合結果をFinderで表示
}
```

**完了条件**: ビルド成功。ViewModel のスケルトンがコンパイルを通る。

---

### Step 4: PDF結合サービス — PDFMergeService の実装

**目的**: PDF結合のコアロジックを分離した純粋なサービスとして実装

**作成ファイル**: `Services/PDFMergeService.swift`

**実装内容**:
```swift
struct PDFMergeService {
    /// 複数のPDFファイルを結合し、指定パスに保存する
    /// - Parameters:
    ///   - urls: 結合するPDFファイルのURL配列（順序通り）
    ///   - outputURL: 保存先URL
    ///   - progress: 進捗コールバック (0.0〜1.0)
    /// - Throws: PDFMergeError
    static func merge(
        urls: [URL],
        to outputURL: URL,
        progress: @Sendable (Double) -> Void
    ) async throws
}

enum PDFMergeError: LocalizedError {
    case fileNotReadable(URL)
    case passwordProtected(URL)
    case writeFailed(URL)
    case noFiles
}
```

**結合アルゴリズム**:
1. 出力用 `PDFDocument()` を生成
2. 各URLを順にループ:
   a. `PDFDocument(url:)` で読み込み（失敗→エラー）
   b. `isLocked` チェック（ロック→スキップ+通知）
   c. 全ページを `insert(_:at:)` で追記
   d. 進捗を更新
3. `write(to:)` で保存

**完了条件**: ビルド成功。ユニットテスト用にサービス単体でテスト可能な設計。

---

### Step 5: アプリエントリポイント — ウィンドウ設定

**目的**: ウィンドウサイズ、ツールバースタイル、ViewModelの注入を設定

**変更ファイル**: `PDF_UtilsApp.swift`

**変更内容**:
```swift
@main
struct PDF_UtilsApp: App {
    @State private var viewModel = PDFMergeViewModel()

    var body: some Scene {
        WindowGroup {
            ContentView()
                .environment(viewModel)
        }
        .defaultSize(width: 960, height: 640)
        .windowResizability(.contentMinSize)
    }
}
```

**完了条件**: ビルド成功。960×640のウィンドウが表示される。

---

### Step 6: 空状態ビュー（ゼロステート）の実装

**目的**: ファイル未追加時の「最初に見る画面」を実装

**変更ファイル**: `Views/ContentView.swift`
**作成ファイル**: `Views/DropOverlayView.swift`

**実装内容**:
- ウィンドウ全体をドロップゾーンとして構成
- 中央にアイコン + メッセージ（`doc.richtext` 48pt、テキスト2行）
- ツールバーに「ファイル追加」ボタンのみ
- `.onDrop` / `.dropDestination` でPDFファイルの受け入れ
- `.fileImporter` でファイル選択ダイアログ
- ドラッグ中は背景色が `.blue.opacity(0.08)` に変化

**キーボードショートカット**:
- ⌘O → ファイル追加ダイアログ

**完了条件**: ビルド成功。空状態画面が表示される。ファイル追加ダイアログが開く。D&Dでファイルを追加するとViewModelに反映される（コンソール出力で確認）。

---

### Step 7: PDFリストビュー — 左ペインの実装

**目的**: 追加されたPDFファイルの一覧を表示し、選択・並び替え・削除を可能にする

**作成ファイル**:
- `Views/PDFListView.swift`
- `Views/PDFListRowView.swift`

**変更ファイル**: `Views/ContentView.swift`

**PDFListRowView の実装**:
- サムネイル（44×56pt、角丸2pt、薄い影）
- ファイル名（`.body` `.medium`、長い名前は省略+ツールチップ）
- ページ数・サイズ（`.caption` `.secondary`）

**PDFListView の実装**:
- `List` + `ForEach` でリスト表示
- `.onMove` で並び替え対応
- 選択状態は `viewModel.selectedItemID` にバインド
- `.onDeleteCommand` で ⌘⌫ 対応

**ContentView の変更**:
- `NavigationSplitView` に切り替え
  - `isEmpty` → ゼロステート（Step 6のビュー）
  - `!isEmpty` → サイドバー: `PDFListView`、detail: プレビュー（Step 8で実装、仮で `Text` を配置）
- ツールバーにボタン追加（削除、全クリア、結合）
  - 結合ボタンは `.borderedProminent`、右寄せ
  - ファイルが0件の場合は削除・全クリア・結合ボタンを非表示

**アニメーション**:
- ファイル追加: `.spring(response: 0.3, dampingFraction: 0.8)` でスライドイン
- ファイル削除: `.easeOut(duration: 0.25)` でフェード+縮小

**完了条件**: ビルド成功。PDFファイルをD&Dまたはダイアログで追加するとリストに表示される。並び替え・削除が動作する。

---

### Step 8: PDFプレビュー — 右ペインの実装

**目的**: 選択中のPDFをページプレビュー表示する

**作成ファイル**: `Views/PDFPreviewView.swift`

**実装内容**:
- `NSViewRepresentable` で `PDFView` をラップ
- `autoScales = true` で自動フィット
- `displayMode = .singlePage` デフォルト
- 未選択時は「PDFを選択してプレビュー」テキスト（`.secondary`）
- `viewModel.selectedItem` の変更に応じて `PDFDocument` を切り替え

**ContentView への統合**:
- `NavigationSplitView` の `detail:` に `PDFPreviewView` を配置

**完了条件**: ビルド成功。リストでPDFを選択するとプレビューが表示される。ページスクロール、ズームが動作する。

---

### Step 9: ステータスバーの実装

**目的**: ウィンドウ下部にファイル数・合計ページ数・プログレスバーを表示

**変更ファイル**: `Views/ContentView.swift`

**実装内容**:
- `ContentView` の最下部に `HStack` でステータスバーを配置
- 左: 「📄 {n}ファイル · 合計{m}ページ」（`.caption` `.secondary`）
- 右: 結合処理中のみ `ProgressView` を `.transition(.opacity)` で表示
- 結合完了時: ✅ + 「結合完了」テキストが3秒間表示後フェードアウト

**完了条件**: ビルド成功。ファイル追加/削除に応じてステータスがリアルタイム更新される。

---

### Step 10: PDF結合機能の統合

**目的**: ViewModelと結合サービスを接続し、結合ワークフロー全体を完成させる

**変更ファイル**:
- `ViewModels/PDFMergeViewModel.swift`（`merge()` メソッドの本実装）
- `Views/ContentView.swift`（結合ボタンのアクション接続）

**ワークフロー**:
1. 結合ボタン押下 → `NSSavePanel` を表示（デフォルト名: `Merged.pdf`）
2. ユーザーが保存先を選択 → `viewModel.merge()` を `Task` で呼び出し
3. 結合中:
   - `isMerging = true`、ツールバーボタンを無効化
   - プログレスバーを表示、`mergeProgress` をリアルタイム更新
4. 成功時:
   - `showSuccessBanner = true`、`savedFileURL` を保持
   - 成功バナーをスライドイン表示（5秒後にフェードアウト）
   - 「Finderで表示」ボタン
5. 失敗時:
   - `alertMessage` にエラー内容を設定
   - `.alert` で表示

**キーボードショートカット**:
- ⌘S → 結合実行

**完了条件**: ビルド成功。複数PDFを追加→結合→保存が動作する。結合後のPDFが正しく開ける。

---

### Step 11: エラーハンドリングとエッジケース

**目的**: 堅牢性の確保

**対応項目**:

| # | ケース | 対応 |
|---|--------|------|
| 1 | 非PDFファイルのD&D | `.dropDestination` で `UTType.pdf` のみ受理。無視される |
| 2 | 破損PDFの追加 | `PDFDocument(url:)` が `nil` → エラーAlert表示、リストに追加しない |
| 3 | パスワード付きPDF | `isLocked == true` → Alert表示「{ファイル名}はパスワードで保護されています」 |
| 4 | 同一ファイルの重複追加 | URLの重複チェック → 既に追加済みの場合はスキップ+通知 |
| 5 | 結合対象が1件のみ | 結合ボタンを無効化（`canMerge = items.count >= 2`） |
| 6 | 結合中のUI操作 | `isMerging` 中はツールバーボタンを `.disabled(true)` |
| 7 | 保存先の書き込み権限なし | `PDFMergeError.writeFailed` → エラーAlert |

**変更ファイル**:
- `ViewModels/PDFMergeViewModel.swift`
- `Models/PDFItem.swift`（ファクトリメソッドにバリデーション）
- `Views/ContentView.swift`（`.alert` 表示の追加）

**完了条件**: 上記全ケースで適切なエラーメッセージが表示され、アプリがクラッシュしない。

---

### Step 12: アクセシビリティとポリッシュ

**目的**: 全ユーザーが使えるアプリに仕上げる

**対応項目**:

| カテゴリ | 対応内容 |
|---------|---------|
| VoiceOver | 全操作要素に `.accessibilityLabel` 付与 |
| VoiceOver | リスト行に `"{ファイル名}、{ページ数}ページ、{サイズ}"` を設定 |
| VoiceOver | ステータスバーに `.accessibilityLiveRegion(.polite)` |
| キーボード | Tab 移動の順序を検証 |
| キーボード | ⌘A でリスト全選択 |
| 並び替え | `.accessibilityAction` で「上に移動」「下に移動」を提供 |
| プレビュー | ズーム ⌘+/⌘- のショートカットを検証 |
| 最終調整 | アニメーションタイミングの微調整 |
| 最終調整 | ウィンドウ最小サイズ 600×400 の検証 |
| 最終調整 | ダークモード / ライトモードの表示確認 |

**完了条件**: VoiceOverで全機能を操作可能。ダーク/ライト両モードで表示崩れなし。

---

### 実装順序の全体像

```
Step 1   テンプレート除去
  │       ↓ クリーンな状態
Step 2   PDFItem モデル
  │       ↓ データ型が確定
Step 3   ViewModel スケルトン
  │       ↓ 状態管理の器が完成
Step 4   PDF結合サービス
  │       ↓ コアエンジン完成
Step 5   アプリエントリポイント設定
  │       ↓ ViewModel注入、ウィンドウ設定
Step 6   空状態ビュー + D&D + ファイル追加
  │       ↓ ファイルが追加できるようになる
Step 7   PDFリストビュー（左ペイン）
  │       ↓ 追加したファイルが見える・操作できる
Step 8   PDFプレビュー（右ペイン）
  │       ↓ 選択したPDFの中身が見える
Step 9   ステータスバー
  │       ↓ 状態が一目でわかる
Step 10  PDF結合機能の統合
  │       ↓ コア機能が完成
Step 11  エラーハンドリング
  │       ↓ 堅牢性の確保
Step 12  アクセシビリティ + ポリッシュ
          ↓ 出荷品質
```

### 各ステップの所要時間見積もり

| Step | 内容 | 見積もり |
|------|------|---------|
| 1 | テンプレート除去 | 5分 |
| 2 | PDFItem モデル | 15分 |
| 3 | ViewModel スケルトン | 20分 |
| 4 | PDF結合サービス | 30分 |
| 5 | アプリエントリポイント | 5分 |
| 6 | 空状態ビュー + D&D | 30分 |
| 7 | PDFリストビュー | 45分 |
| 8 | PDFプレビュー | 30分 |
| 9 | ステータスバー | 15分 |
| 10 | 結合機能統合 | 30分 |
| 11 | エラーハンドリング | 30分 |
| 12 | アクセシビリティ + ポリッシュ | 30分 |
| | **合計** | **約4.5時間** |

---

## 12. 実装手順（v1.2.0 分割機能）

全8ステップ。v1.0/v1.1の既存コードベースの上に追加する。
各ステップ完了時にビルド確認を行い、コンパイルが通る状態を維持する。
既存の結合機能に一切の回帰を発生させないこと。

---

### Step 1: AppMode と モード切替UI

**目的**: 結合/分割のモード切替の土台を作る

**作成ファイル**: なし（既存ファイルの変更のみ）

**変更内容**:

`PDFMergeViewModel.swift`:
```swift
enum AppMode: String, CaseIterable, Identifiable {
    case merge
    case split
    var id: String { rawValue }
}

// ViewModel に追加:
var appMode: AppMode = .merge
```

`ContentView.swift`:
- ツールバーに `Picker("モード", selection: $viewModel.appMode)` を `.pickerStyle(.segmented)` で追加
- 左端に配置（`.placement: .navigation`）
- モード切替時: 結合モードのUIと分割モードのUIを `switch` で出し分け
- 分割モード側はまず `Text("分割モード")` プレースホルダーを表示

**完了条件**: ビルド成功。ツールバーのセグメントコントロールでモード切替ができる。結合モードは既存動作が維持される。

---

### Step 2: SplitConfig モデル

**目的**: 分割設定を表す型安全なモデルを作成

**作成ファイル**: `Models/SplitConfig.swift`

**モデル設計**:
```swift
import Foundation

enum SplitMethod: String, CaseIterable, Identifiable {
    case pageRanges   // ページ範囲指定
    case everyPage    // 1ページずつ
    case oddEven      // 偶数/奇数
    var id: String { rawValue }
}

struct SplitConfig {
    var method: SplitMethod = .pageRanges
    var rangeText: String = ""

    /// テキストからページ範囲グループを解析
    func computeGroups(totalPages: Int) -> Result<[[Int]], SplitConfigError>
}

enum SplitConfigError: LocalizedError {
    case invalidFormat(String)
    case pageOutOfRange(Int, max: Int)
    case overlappingRanges
    case emptyResult
}
```

**重要な設計判断**:
- `computeGroups` は純粋関数。UIに依存しない
- ページ番号は1-based（ユーザー入力）→ 内部で0-basedに変換
- 未指定ページは自動的に末尾グループに追加
- 偶数/奇数モードは `rangeText` を無視

**完了条件**: ビルド成功。`computeGroups` が各分割方法で正しいグループを返す。

---

### Step 3: PDFSplitService の実装

**目的**: PDF分割のコアロジックを分離した純粋なサービスとして実装

**作成ファイル**: `Services/PDFSplitService.swift`

**実装内容**:
```swift
enum PDFSplitService {
    /// PDFファイルを分割し、指定フォルダに複数ファイルとして保存する
    /// - Parameters:
    ///   - url: 分割元PDFのURL
    ///   - groups: ページインデックスのグループ配列（0-based）
    ///   - outputDirectory: 保存先ディレクトリURL
    ///   - baseName: 出力ファイルのベース名
    ///   - progress: 進捗コールバック (0.0〜1.0)
    /// - Returns: 生成されたファイルのURL配列
    static func split(
        url: URL,
        groups: [[Int]],
        to outputDirectory: URL,
        baseName: String,
        progress: @Sendable (Double) -> Void
    ) async throws -> [URL]
}

enum PDFSplitError: LocalizedError {
    case fileNotReadable(URL)
    case passwordProtected(URL)
    case writeFailed(URL)
    case noGroups
}
```

**分割アルゴリズム**:
1. 元PDFを `PDFDocument(url:)` で読み込み（失敗→エラー）
2. `isLocked` チェック
3. 各グループごとにループ:
   a. 新規 `PDFDocument()` を生成
   b. グループ内のページインデックスを順に `page(at:)` → `insert(_:at:)`
   c. `{baseName}_part{n}.pdf` として `write(to:)` で保存
   d. 進捗を更新
4. 生成されたURL配列を返却

**`PDFMergeService` との共通点**: `PDFDocument` の読み込み・ページ挿入・書き出しパターンは結合サービスと同一。ただしサービスは分離を維持する（過度な共通化は避ける）

**完了条件**: ビルド成功。サービス単体でテスト可能な設計。

---

### Step 4: ViewModel への分割状態追加

**目的**: 分割モードの状態管理をViewModelに追加

**変更ファイル**: `ViewModels/PDFMergeViewModel.swift`

**追加プロパティ**:
```swift
// --- 分割モード状態 ---
var splitSourceItem: PDFItem? = nil
var splitConfig: SplitConfig = SplitConfig()
var isSplitting: Bool = false
var splitProgress: Double = 0.0
var splitOutputURLs: [URL] = []
var splitOutputDirectory: URL? = nil

// --- 算出プロパティ ---
var canSplit: Bool  // splitSourceItem != nil && 有効な分割設定
var splitGroups: Result<[[Int]], SplitConfigError>  // リアルタイム計算

// --- 操作 ---
func setSplitSource(url: URL)       // 分割元PDF読み込み
func split() async                  // 分割実行（非同期）
func revealSplitOutputInFinder()    // 出力フォルダをFinderで表示
```

**モード切替時の挙動**:
- `.merge` → `.split`: `pdfItems` はクリアしない（戻った時に復元）
- `.split` → `.merge`: `splitSourceItem` はクリアしない（戻った時に復元）
- 処理中（`isMerging` / `isSplitting`）のモード切替は無効化

**完了条件**: ビルド成功。分割関連のプロパティとスケルトンメソッドがコンパイルを通る。

---

### Step 5: SplitConfigView — 左ペインの実装

**目的**: 分割モードの左ペインUI（ファイル情報 + 分割設定）を実装

**作成ファイル**: `Views/SplitConfigView.swift`

**実装内容**:
```
┌──────────────────────────┐
│  ┌────┐                  │
│  │ 🖼 │  report.pdf      │  ← PDFListRowView を再利用
│  │    │  24ページ · 4.2MB │
│  └────┘                  │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │
│                          │
│  分割方法                │  ← Section ヘッダ
│  ● ページ範囲指定         │  ← Picker(.radioGroup)
│  ○ 1ページずつ           │
│  ○ 偶数/奇数             │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─   │
│                          │
│  ページ範囲              │  ← pageRanges 選択時のみ表示
│  ┌──────────────┐ /24    │  ← TextEditor + 総ページ数
│  │ 1-4, 5-10,   │        │
│  │ 11-24         │        │
│  └──────────────┘        │
│                          │
│  ⚠️ ページ範囲が不正です   │  ← バリデーションエラー時のみ
│                          │
└──────────────────────────┘
```

**デザイン詳細**:
- ファイル情報部分は `PDFListRowView` と同じデザイン言語を再利用
- 分割方法は `Picker` + `.radioGroup` スタイル（macOSネイティブ）
- ページ範囲入力は `TextField`（プレースホルダー: `"1-5, 6-10, 11-24"`）
- 入力右に総ページ数を `.secondary` `.caption` で表示
- バリデーションエラーは入力欄下に `.red` `.caption` で表示
- 「1ページずつ」「偶数/奇数」選択時はページ範囲入力を非表示（`.transition(.opacity)`）

**完了条件**: ビルド成功。分割モードで左ペインに設定UIが表示される。分割方法の切替、ページ範囲入力が動作する。

---

### Step 6: SplitPreviewView — 右ペインの実装

**目的**: 分割結果をページサムネイルグリッドで視覚的にプレビューする

**作成ファイル**: `Views/SplitPreviewView.swift`

**実装内容**:
- `ScrollView` + `LazyVGrid` でページサムネイルをグリッド表示
- サムネイルサイズ: 80×104pt（A4比率、v1.1.0のページビューと同一）
- 各サムネイル左下にページ番号を表示（`.caption2` 白文字 + 半透明黒背景）
- 分割グループ間に区切り線（`Divider` + グループラベル `"Part 1 (4ページ)"` `.caption` `.secondary`）
- 各グループに薄い背景色を交互に適用（`.quaternary` / `.clear`）で視覚的に区別
- ファイル未選択時: 「PDFファイルを選択してプレビュー」テキスト（`.secondary`）
- 分割設定が無効な場合: サムネイルは表示するが区切り線なし + 上部に警告メッセージ

**サムネイル生成**:
- `PDFItem` の既存サムネイル生成ロジックを拡張（または `PDFPage` から直接描画）
- 全ページのサムネイルを初回読み込み時に生成しキャッシュ

**完了条件**: ビルド成功。分割モードでPDFを選択すると右ペインに全ページのサムネイルがグリッド表示される。分割設定に応じて区切り線が正しい位置に表示される。

---

### Step 7: 分割実行の統合

**目的**: ViewModelと分割サービスを接続し、分割ワークフロー全体を完成させる

**変更ファイル**:
- `ViewModels/PDFMergeViewModel.swift`（`split()` メソッドの本実装）
- `Views/ContentView.swift`（分割ボタンのアクション接続、成功バナー対応）

**ワークフロー**:
1. 分割ボタン押下 → `NSOpenPanel`（ディレクトリ選択モード）を表示
2. ユーザーが保存先フォルダを選択 → `viewModel.split()` を `Task` で呼び出し
3. 分割中:
   - `isSplitting = true`、ツールバーボタンを無効化
   - プログレスバーを表示、`splitProgress` をリアルタイム更新
4. 成功時:
   - `showSuccessBanner = true`、`splitOutputDirectory` を保持
   - 成功バナー: 「✅ {n}ファイルに分割しました」+「Finderで表示」ボタン
5. 失敗時:
   - `alertMessage` にエラー内容を設定
   - `.alert` で表示

**⌘S の共有**:
- `⌘S` はアクティブモードに応じて結合 or 分割を実行
- `canMerge` / `canSplit` に応じてボタン無効化

**NSSavePanel vs NSOpenPanel**:
- 結合: `NSSavePanel`（1ファイル出力）
- 分割: `NSOpenPanel` + `canChooseDirectories = true` + `canChooseFiles = false`（フォルダ選択）

**完了条件**: ビルド成功。PDFを選択→分割設定→分割実行→保存が動作する。分割後のPDFファイルが正しく開ける。

---

### Step 8: エラーハンドリング・アクセシビリティ・ポリッシュ

**目的**: 分割機能の堅牢性とアクセシビリティを確保

**エラーハンドリング**:

| # | ケース | 対応 |
|---|--------|------|
| 1 | 不正なページ範囲入力 | リアルタイムバリデーションで赤字エラー表示、分割ボタン無効化 |
| 2 | ページ番号が総ページ数を超過 | `SplitConfigError.pageOutOfRange` → エラーテキスト |
| 3 | 範囲の重複 | `SplitConfigError.overlappingRanges` → エラーテキスト |
| 4 | 空の入力 | 分割ボタンを無効化（静かに待つ） |
| 5 | 保存先フォルダの書き込み権限なし | `PDFSplitError.writeFailed` → エラーAlert |
| 6 | 分割中のUI操作 | `isSplitting` 中はツールバーボタン・設定UIを `.disabled(true)` |
| 7 | 分割中のモード切替 | モード切替ピッカーを `.disabled(true)` |

**アクセシビリティ**:

| 要素 | 対応 |
|------|------|
| モード切替 | `"動作モード"` をグループラベルに |
| 分割方法ラジオ | 各選択肢に明確な `.accessibilityLabel` |
| ページ範囲入力 | `"分割ページ範囲"` + バリデーション結果を `.accessibilityHint` に |
| 分割プレビュー | 各グループに `"パート{n}、{m}ページ"` をラベル設定 |
| 分割ボタン | `"PDFを分割"` + 出力ファイル数を `.accessibilityValue` に |
| ステータスバー | 分割情報を `.accessibilityLiveRegion(.polite)` で通知 |

**ポリッシュ**:
- ダークモード / ライトモードの表示確認
- モード切替アニメーションの微調整
- 分割プレビューのスクロールパフォーマンス検証（100ページ超のPDF）
- 分割境界線の色とコントラスト確認

**完了条件**: 全エラーケースで適切なメッセージが表示されアプリがクラッシュしない。VoiceOverで分割機能の全操作が可能。

---

### 実装順序の全体像（v1.2.0）

```
Step 1   AppMode + モード切替UI
  │       ↓ モード切替の骨格が完成
Step 2   SplitConfig モデル
  │       ↓ 分割設定の型が確定
Step 3   PDFSplitService
  │       ↓ 分割エンジン完成
Step 4   ViewModel への分割状態追加
  │       ↓ 状態管理の器が完成
Step 5   SplitConfigView（左ペイン）
  │       ↓ 分割設定UIが動作
Step 6   SplitPreviewView（右ペイン）
  │       ↓ 分割結果が見える
Step 7   分割実行の統合
  │       ↓ コア機能が完成
Step 8   エラーハンドリング + アクセシビリティ
          ↓ 出荷品質
```

### 各ステップの所要時間見積もり

| Step | 内容 | 見積もり |
|------|------|--------|
| 1 | AppMode + モード切替UI | 20分 |
| 2 | SplitConfig モデル | 25分 |
| 3 | PDFSplitService | 30分 |
| 4 | ViewModel 分割状態追加 | 25分 |
| 5 | SplitConfigView（左ペイン） | 40分 |
| 6 | SplitPreviewView（右ペイン） | 45分 |
| 7 | 分割実行の統合 | 35分 |
| 8 | エラーハンドリング + ポリッシュ | 30分 |
| | **合計** | **約4時間** |

## 13. 将来の拡張候補（v2以降）

- ページ回転機能
- ページ抽出機能（選択ページだけを新PDFとして書き出し）
- PDF圧縮（画像ダウンサンプリングによるファイルサイズ削減）
- 透かし（Watermark）追加
- PDFメタデータ（タイトル・著者）の編集
- バッチ処理（複数セットの結合・分割を一括実行）
- Finder Quick Action / Services メニューへの統合
